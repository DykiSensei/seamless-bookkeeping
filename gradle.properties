# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# AndroidX 必须启用（AGP 8.x 仍需要显式声明）
android.useAndroidX=true
# 启用非传递的 R 类（更快的编译）
android.nonTransitiveRClass=true
# Kotlin code style
kotlin.code.style=official
